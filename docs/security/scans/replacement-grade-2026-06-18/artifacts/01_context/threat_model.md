# Per-Scan Threat Model

This scan uses the repository threat model at `docs/security/threat_model.md` as its source of
truth. The scoped surfaces are upload/parsing/storage, patch/export/redaction, templates/forms,
approvals/bulk-send, editor-session APIs, and the deterministic DocumentOpsAgent planning API.

