"""docos — Cross-Format Document OS backend.

The package is organized around a canonical document model (``docos.model``) that
every service reads from and writes to. See ``docos.services`` for the five
service boundaries and ``backend/README.md`` for the package map.
"""

__version__ = "0.1.0"
