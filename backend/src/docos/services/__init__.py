"""The five document services. Each exposes an interface in ``<service>/interface.py``.

Services depend on one another only through these interfaces and the canonical
model, so formats, providers, and scanners stay pluggable.
"""
